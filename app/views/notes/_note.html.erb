<p>
  <%= note.user.email %>: <%= note.body %>
  <% if current_user == note.user %>
    <%= button_to "Delete", note, method: "delete", class: "btn btn-danger", data: {confirm: "Are you sure?"}%>
  <% end %>
</p>
