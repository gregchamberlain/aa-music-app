<h1>Bands</h1>
<table class="table">
  <thead>
    <td>Name</td>
  </thead>
  <tbody>
    <% @bands.each do |band| %>
      <tr>
        <td><%= link_to band.name, band %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "New Band", new_band_url, class: "btn btn-success" %>
